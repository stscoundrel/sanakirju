const XML_PATH = `${__dirname}/../../resources/xml/kksxml/`;

export const XML_FOLDERS: string[] = [
  `${XML_PATH}kks1`,
  `${XML_PATH}kks2`,
  `${XML_PATH}kks3`,
  `${XML_PATH}kks4`,
  `${XML_PATH}kks5`,
  `${XML_PATH}kks6`,
];

export default {
  XML_FOLDERS,
};

const XML_PATH = `${__dirname}/../../resources/xml/kksxml/`

const XML_FOLDERS = [
  `${XML_PATH}kks1`,
  `${XML_PATH}kks2`,
  `${XML_PATH}kks3`,
  `${XML_PATH}kks4`,
  `${XML_PATH}kks5`,
  `${XML_PATH}kks6`,
]

module.exports = {
  XML_FOLDERS,
}
